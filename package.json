{
  "name": "hls-proxy-ready",
  "version": "1.0.0",
  "description": "HLS proxy ready for Render (server.js at project root)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "node-fetch": "^2.6.9"
  }
}